{% extends 'GradeWebBundle::template.html.twig' %}

{% block content_content -%}
    <h1>Grade list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Value</th>
                <th>Guid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('grade_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.guid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('grade_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('grade_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('grade_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
